# Changelog

本项目的重要变更记录在此文件中。

## [Unreleased]

## [1.1.0] - 2026-09-03

### 新增

- 人像与商品/通用物体双模型本地 AI 抠图。
- AI 模型按需下载、进度显示、大小与 SHA256 校验、本机删除和离线推理。
- 命名参数方案的新建、应用、更新、重命名和删除。
- “恢复默认参数”“清空队列”和“开启下一批”操作。
- 文件夹失败项归档，以及 UTF-8 格式的失败原因 TXT/CSV 清单。
- 多尺寸专业应用图标。
- 完整配置包导入导出，覆盖当前参数、外观设置和全部命名方案。

### 改进

- 严格执行目标文件大小，无法达标时不再保留超标结果。
- 单文件默认输出为同目录 `原名-已处理`，文件夹默认输出到同级 `原文件夹-已处理`。
- 尺寸调整默认关闭，只有启用后才校验宽度或高度。
- 队列直接显示失败环节、具体原因、文件结果和处理建议。
- 深色模式下拉框使用完整自定义模板，提高弹出选项、悬停和选中状态的可读性。
- AI 预览与正式处理使用相同的本地抠图引擎。
- ONNX 会话按模型复用，预览防抖且批处理期间暂停刷新，降低卡顿和内存抖动。
- 批处理自动并发上限调整为 2。
- 安装器源码切换为 Inno Setup 6.6 深色向导样式。
- 用户可见产品名称统一为“苏影枢”，内部数据目录保持兼容。

### 修复

- 修复设置 1 MB 后仍保留 2 MB 以上结果的问题。
- 修复上一轮队列、预览和异步进度影响下一批的问题。
- 修复 AI 模型缺失时仅显示笼统处理错误的问题。
- 修复深色下拉选项文字与背景对比不足的问题。

### 发布说明

- 正式版本号为 `v1.1.0`。
- AI 模型不随安装包或 Release 附件分发，由用户在应用内按需下载。

## [1.0.0] - 2026-09-02

首个正式公开版本。

### 新增

- Windows 11 x64 本地图片批处理工作台。
- 图片、文件夹、拖放和子文件夹扫描导入。
- JPEG、PNG、WebP、BMP、TIFF 输入支持。
- JPEG/WebP 目标大小压缩和质量搜索。
- PNG 无损压缩、可选有损量化和自动缩放。
- 尺寸调整、比例锁定、裁剪锚点和背景补边。
- 原图与处理后实时预览。
- EXIF、ICC 和 GPS 元数据控制。
- 原目录新文件、指定目录和确认后覆盖三种输出方式。
- 批任务暂停、继续、安全取消和失败项重试。
- 浅色、深色和跟随系统主题。
- 本地配置、隐私安全日志和日志轮转。

### 安全性

- 覆盖原文件前写入临时文件并校验可读性。
- 使用原子替换降低处理中断导致的文件损坏风险。
- 新文件输出自动避让重名。
- 取消任务时清理未完成的输出文件。
- 多页 TIFF 禁止覆盖原文件。
- GPS 信息默认删除。

### 交付

- Windows 11 x64 自包含发布。
- Inno Setup 当前用户级安装包。
- 产品介绍与安装说明 HTML/PDF 文档。
- 安装、启动、退出和卸载冒烟测试。
- 74 项自动化测试全部通过。

### 已知限制

- HEIC、AVIF 暂不支持。
- ARM64 仅为实验性支持。
- AI 抠图和本地 AI 模型尚未实现。
- 安装包尚未代码签名。
